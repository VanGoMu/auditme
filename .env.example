# .env.example - Environment Variables Template
# Copy this file to .env and customize the values

# InfluxDB Configuration
INFLUXDB_VERSION=2.7-alpine
INFLUXDB_DB=system_logs
INFLUXDB_ADMIN_USER=admin
INFLUXDB_ADMIN_PASSWORD=CHANGE_ME_SecurePassword123
INFLUXDB_TOKEN=CHANGE_ME_my-super-secret-influxdb-token-here
INFLUXDB_ORG=debian_monitoring
INFLUXDB_BUCKET=system_logs

# Fluent Bit Configuration
FLUENTBIT_VERSION=3.1
FLUENTBIT_LOG_LEVEL=info

# Grafana Configuration
GRAFANA_VERSION=latest
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_GrafanaAdmin123
GRAFANA_PORT=3000

# User/Group IDs (auto-detected if not set)
# UID and GID for running containers with your user permissions
# UID=1000
# GID=1000

# Retention Policy (days)
RETENTION_SYSTEM_LOGS=60

# Network
MONITORING_NETWORK=monitoring-net

# ============================================
# IMPORTANT SECURITY NOTES:
# ============================================
# 1. CHANGE the INFLUXDB_ADMIN_PASSWORD before first use
# 2. CHANGE the INFLUXDB_TOKEN to a strong random value
# 3. DO NOT commit the .env file to version control
# 4. Keep these credentials secure and private
#
# Generate a secure token with:
#   openssl rand -base64 32
# Or:
#   cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 64 | head -n 1
